/// <reference path="observer-lite.d.ts" />
declare module Rx {
    module internals {
        interface ScheduledObserver<T> extends Observer<T> {
            ensureActive(): void;
        }
    }
}

declare module Rx {
    /**
    * Used to pause and resume streams.
    */
    interface Pauser {
        /**
         * Pauses the underlying sequence.
         */
        pause(): void;
        /**
        * Resumes the underlying sequence.
        */
        resume(): void;
    }
}
